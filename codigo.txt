HTML
    function listarExercicio() {
        fetch("/avisos/listarExercicio").then(function (resposta) {
            if (resposta.ok) {
                if (resposta.status == 204) {
                    // var feed = document.getElementById("feed_container");
                    // var mensagem = document.createElement("span");
                    mensagem.innerHTML = "Nenhum resultado encontrado."
                    feed.appendChild(mensagem);
                    throw "Nenhum resultado encontrado!!";
                }

                resposta.json().then(function (resposta) {
                    console.log("Dados recebidos: ", JSON.stringify(resposta));
                    listExercicio = JSON.stringify(resposta);
                    const listaExercicios = new Map();
                    dados.forEach(registro => {
                        // Substitua 'nome_exercicio' pelo campo correto do backend, se necessário
                        if (registro.nome) {
                            listaExercicios.set(String(registro.nome), registro.nome);
                        }
                    });

                    listaExercicios.forEach((nome) => {
                        const option = document.createElement('option');
                        option.value = nome;
                        option.textContent = nome;
                        selectExercicio.appendChild(option);
                    });
                })
            }
        })
    }

    MODEL
    function listarExercicio() {
    console.log("ACESSEI O AVISO  MODEL \n \n\t\t >> Se aqui der erro de 'Error: connect ECONNREFUSED',\n \t\t >> verifique suas credenciais de acesso ao banco\n \t\t >> e se o servidor de seu BD está rodando corretamente. \n\n function listarExercicio()");
    var instrucaoSql = `
        SELECT nome FROM exercicio;
    `;
    console.log("Executando a instrução SQL: \n" + instrucaoSql);
    return database.executar(instrucaoSql);
}

CONTROLER
function listarExercicio(req, res) {
    avisoModel.listarExercicio().then(function (resultado) {
        if (resultado.length > 0) {
            res.status(200).json(resultado);
        } else {
            res.status(204).send("Nenhum resultado encontrado!")
        }
    }).catch(function (erro) {
        console.log(erro);
        console.log("Houve um erro ao buscar os avisos: ", erro.sqlMessage);
        res.status(500).json(erro.sqlMessage);
    });
}


ROUTE:
router.get("/listarExercicio", function (req, res) {
    avisoController.listarExercicio(req, res);
});
